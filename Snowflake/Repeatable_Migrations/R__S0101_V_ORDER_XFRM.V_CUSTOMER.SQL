CREATE OR REPLACE VIEW V_CUSTOMER (
	CUSTOMER_ID,
	ADDRESS,
	PHONE_NO,
	CATEGORY,
	STATUS,
	TOTAL_ORDERS,
	TOTAL_INVOICE_AMOUNT,
	LOYALITY_POINTS,
	CREATE_DATE,
	CREATE_USER
) AS SELECT * FROM T_CUSTOMER;