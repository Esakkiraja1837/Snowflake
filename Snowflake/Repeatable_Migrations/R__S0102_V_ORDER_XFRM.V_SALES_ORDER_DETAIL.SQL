CREATE OR REPLACE VIEW V_SALES_ORDER_DETAIL (
    ORDER_ID,
	ORDER_DATE,
	CUSTOMER_ID,
	ITEM_ID,
	QTY,
    PRICE,
	TOTAL_AMOUNT,
	CREATE_DATE,
	CREATE_USER
) AS SELECT * FROM T_SALES_ORDER_DETAIL;